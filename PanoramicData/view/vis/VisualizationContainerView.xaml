<local:MovableElement x:Class="PanoramicData.view.vis.VisualizationContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lib="clr-namespace:PixelLab.Wpf.Transitions;assembly=PixelLab.Wpf"
             xmlns:local="clr-namespace:PanoramicData.view.vis"
             mc:Ignorable="d" 
             MinWidth="150" MinHeight="100"
             d:DesignHeight="300" d:DesignWidth="300"
             Width="{Binding Size.X}" Height="{Binding Size.Y}"
             Canvas.Top="{Binding Position.Y}" Canvas.Left="{Binding Position.X}">
    <Grid Name="mainGrid">
              
        <lib:TransitionPresenter 
                x:Name="transitionPresenter">
            <lib:TransitionPresenter.Transition>
                <lib:RotateTransition 
                    x:Name="rotationTransition"
                    TextSearch.Text="3D Spin" 
                    Duration="0:0:0.0" FieldOfView="40" 
                    Angle="0" Contained="false" Direction="Left"/>
            </lib:TransitionPresenter.Transition>
        </lib:TransitionPresenter>

    </Grid>
</local:MovableElement>
