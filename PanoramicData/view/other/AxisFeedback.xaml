<UserControl x:Class="PanoramicData.view.other.AxisFeedback"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="300">
    <Grid >
        <InkPresenter
            x:Name="inkPresenter"/>
        <Border 
            Name="yBorder"
            Padding="3"
            Background="White"
            BorderBrush="Black" BorderThickness="1" 
            HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBox Name="yAxis" Text="Y - Axis" BorderThickness="0" Background="Transparent"/>
        </Border>
        
        <Border 
            Name="xBorder"
            Padding="3"
            Background="White"
            BorderBrush="Black" BorderThickness="1" 
            HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <TextBox Name="xAxis" Text="X - Axis" BorderThickness="0" Background="Transparent"/>
        </Border>
    </Grid>
</UserControl>
